import { Scene } from "@/components/portfolio/Scene";
import { SectionTitle } from "@/components/ui/SectionTitle";

export function NotesScene() {
  return <Scene id="notes" eyebrow="Notes / references" className="flex flex-col"><SectionTitle id="notes-title" className="mt-auto max-w-4xl">Fragments, observations, and what comes next.</SectionTitle></Scene>;
}
